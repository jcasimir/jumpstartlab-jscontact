- semantic_form_for @attachment, :html => {:multipart => true} do |f|
  = f.error_messages

  - f.inputs do
    = f.input :title
    = f.input :file, :as => :file
    = f.input :contacts, :as => :check_boxes

  = f.buttons