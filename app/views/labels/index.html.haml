- title "Labels"

%table
  %tr
    %th Name
  - for label in @labels
    %tr
      %td= h label.name
      %td= link_to 'Show', label
      %td= link_to 'Edit', edit_label_path(label)
      %td= link_to 'Destroy', label, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Label", new_label_path
